Go to AWS Console EC2
Click Launch Instance
Select:

AMI: Ubuntu
Instance type: t3 medium (Free Tier)

Create/select key pair
Allow:

SSH port

Launch the instance
Connect to EC2



Terraformt

>>sudo su

>>wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg-dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gog

>>echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep op (?<=UBUNTU_CODENAME=). /etc/os-release || lsb release -cs) main | sudo tee /etc/apt/sources.list.d/hashicorp.list

>>sudo apt update && sudo apt install terraform-y

>>terraform--version

>>mkdir project

>>cd project


Go to AWS Console
Search IAM
Click Users
Create user:
Name: anything (e.g., terraform-user)
Permissions: AdministratorAccess
Create user

Go to:
Security Credentials
Click Create Access Key
COL

Save:
Access Key
Secret Key



>>vi connection.tf

provider "aws" {
  region = "us-east-1"
  access key = "AKIAY2NBQTWQZ5WALEFR"
  secret key = "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"
}


>>vi ec2.tf

data "aws_ami" "ubuntu" {
  most recent = true
  filter {
     name   = "name"
     values = ["ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*"]
  }

  filter {
    name   = "virtualization-type"
    values = ["hvm"]
  }
  owners = ["099720109477"] # Canonical
}

resource "aws_instance" "example" {
  ami           = data.aws_ami.ubuntu.id
  instance type = "t3.medium"
  tags = {
    Name "HelloWorld"
  }
}




>>terraform init
>>terraform apply --auto-approve



Go to AWS Console → EC2 Check instance list
You should see:
Terraform-Instance running


>>terraform destroy-auto-approve


https://developer.hashicorp.com/terraform/install#linux

https://registry.terraform.io/providers/hashicorp/aws/latest/docs

https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/instance



AKIAY2NBQTWQZ5WALEFR

XgwxVYeJ040TuSG6FfjFbh3t1x3yar+P+Nev1PsV
